If the user names a key, use it. Otherwise read the git branch:
`fix/ar-664-accept-invite` means `AR-664`. Two repos carry branches, `areasim-backend`
and `areasim-webapp`. If they disagree, ask which one.
